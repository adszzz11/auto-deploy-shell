# Nginx Control Configuration
# 이 파일을 source하여 nginx 제어 시 기본값으로 사용

# 기본 서버 IP (로컬호스트)
export NGINX_SERVER_IP="127.0.0.1"

# Nginx upstream 설정 파일 경로
export NGINX_UPSTREAM_CONF="/etc/nginx/conf.d/upstream.conf"

# 기본 config postfix 설정 (빈 문자열이면 추가 설정 없음)
export NGINX_CONFIG_POSTFIX=""

# 예시: 가중치 설정
# export NGINX_CONFIG_POSTFIX="weight=5"

# 예시: 백업 서버로 설정
# export NGINX_CONFIG_POSTFIX="backup"

# 예시: 복합 설정
# export NGINX_CONFIG_POSTFIX="max_fails=3 fail_timeout=30s"

# 백업 파일 보관 경로 (설정 파일 백업 시)
export NGINX_BACKUP_DIR="/var/backups/nginx"

# 로그 레벨 (INFO, WARN, ERROR)
export NGINX_LOG_LEVEL="INFO"
